"""Маркировка демонстрационного стенда `SPEC.md § 8.6` (ADR-008).

Тексты пометок собраны здесь, а не повторены в каждом месте выгрузки.
Пометка — это обещание заказчику: со стенда не уйдёт файл, который можно
принять за настоящий (`CLAUDE.md § 4`). Обещание, записанное в трёх
местах разными словами, перестаёт быть проверяемым.

Маркировка включается **только** `DEMO_DATA=true`. Режим внешнего
подключения на неё не влияет: иначе боевые счета клиентам получили бы
водяной знак навсегда (ADR-008).
"""

from __future__ import annotations

from django.conf import settings

# Первая строка выгрузок без оформления: XLSX, CSV, XML.
# Текст задан дословно в `SPEC.md § 8.6`.
DEMO_NOTE = "DEMO DATA — NOT FOR OPERATIONAL USE"

# Водяной знак на PDF.
DEMO_WATERMARK = "DEMO"

# Префикс темы исходящего сообщения в режиме заглушки.
DEMO_SUBJECT_PREFIX = "[DEMO] "


def is_demo() -> bool:
    """Включена ли маркировка стенда."""
    return bool(settings.DEMO_DATA)
